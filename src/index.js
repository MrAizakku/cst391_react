import React from 'react'; //necessary to run a React program. will appear on nearly every file you create in a React application
import ReactDOM from 'react-dom'; //used only in this index file and applies only to web applications
import App from "./App";

ReactDOM.render(<App />, document.querySelector("#root"));